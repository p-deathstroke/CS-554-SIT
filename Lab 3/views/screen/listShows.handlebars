<main>
	<div style="width:800px; margin:0 auto;">
		<br />
		<h1>{{tag}} </h1>
		<ul>
			{{#each showdata}}
				<li>
					<a href="show/{{id}}" aria-label="show" class="show-link">{{name}}</a>
				</li>
			{{/each}}
		</ul>
	</div>
</main>